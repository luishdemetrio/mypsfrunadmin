<Window x:Class="MyPSFRunScriptDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPSFRunScriptDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="176*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="PSF Run Script demo" 
                   FontSize="20" FontWeight="Bold" FontFamily="Arial"
                   Grid.Row="0"
                   Margin="10,20,10,10"/>

        <Button Content="Click me to close" Grid.Row="1" Margin="100" Click="Button_Click"
                
                />
    </Grid>
</Window>
